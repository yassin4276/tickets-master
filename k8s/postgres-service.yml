apiVersion: v1
kind: Service
metadata:
  name: ticketing-postgres
  namespace: ticketing
spec:
  type: ClusterIP
  selector:
    app: ticketing-postgres
  ports:
    - port: 5432
      targetPort: 5432